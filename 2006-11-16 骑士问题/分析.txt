朝某个方向试探性地跳跃一步再看下一步（取下一步最小可走方向（处里边角问题）），任何一点的坐标加上要试探方向的坐标增量之后，都要判断一下是否已经超出了棋盘的边界。

即：当newi< 0，或newi > 8，或newj < 0，或newj > 8时，都表示已经超出了棋盘的边界，这时，应该放弃该方向，转向试探下一个方向，在不出界的情况下：

如果chess[newi][newj]=0，则表示该方向的前方有通路，可以继续向前跳跃；
如果chess[newi][newj]>0，则表示该格已经走过了，不能再走。放弃该方向，并转向下一个方向进行试探；

问题不同，基本的数据结构就不同
